

from fairseq.models.lstm \
        import LSTMEncoder, \
               LSTMDecoder, \
               LSTMModel

from torch import nn

class MGANDiscriminator(LSTMModel):
    @classmethod
    def build_model(cls, task, args):
        self.model = super().build_model(task, args)
        # Remove the last layer?
